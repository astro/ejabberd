%% -*- Erlang -*-

{include, "vhost.cfg"}.
{include, "localhost.cfg"}.

% Top level configs
%
% XXX - one should be able to override these per-vhost.
{acl, foo, {}}.
{access, bar, {}}.
{shaper, wauug, {}}.
{auth_method, odbc}.
{odbc_server, {mysql}}.
{language, "en"}.
{modules, [{}, {}]}.

% drang will use default config.
% foobar will override the domain_certfile. 
{hosts, [
         {"sturm", vhost},
         "drang",
         {"localhost", localhost},
         {"foobar", vhost, [{domain_certfile, "ssl.pem"}]}
        ]}.

